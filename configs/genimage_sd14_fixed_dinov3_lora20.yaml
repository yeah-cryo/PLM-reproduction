data_root: /mnt/f/datasets/GenImage/stable_diffusion_v_1_4/imagenet_ai_0419_sdv4
genimage_root: /mnt/f/datasets/GenImage
output: outputs/genimage_sd14_fixed_dinov3l_lora_r8_20ep
seed: 42
crop_size: 128
epochs: 20
global_batch_size: 128
micro_batch_size: 32
evaluation_batch_size: 128
workers: 8
optimizer: Adam
learning_rate: 0.0002
betas: [0.9, 0.999]
weight_decay: 0.0002
learning_rate_schedule: constant
precision: bf16
model: dinov3_lora
initialization: imagenet
pretrained_path: /mnt/e/repos/GGGT/pretrained_weight/dinov3-l16
lora:
  rank: 8
  alpha: 16.0
  dropout: 0.0
  start_layer: 1
  end_layer: 24
  targets: [q_proj, k_proj, v_proj, o_proj]
  gradient_checkpointing: true
checkpoint_interval: 5
log_interval: 25
expected_images_per_class: 162000
degradation:
  enabled: false
protocol: fixed pixel mapping, random 128 crop, DINOv3-L/16 attention LoRA, linear head, Adam
